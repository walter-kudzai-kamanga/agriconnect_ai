# Minimal requirements for core functionality
# Use this if full requirements.txt fails to install

# Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0

# Database & ORM
sqlalchemy==2.0.23
alembic==1.12.1

# Basic data handling
numpy==1.24.3
pandas==2.0.3

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utilities
requests==2.31.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dateutil==2.8.2
pytz==2023.3.post1
httpx==0.25.0
aiohttp==3.8.1

# Optional - install separately if needed
# tensorflow==2.15.0  # Requires distutils workaround for Python 3.12
# prophet==1.1.5      # Requires distutils workaround for Python 3.12
# geopandas==0.14.0   # Requires distutils workaround for Python 3.12

